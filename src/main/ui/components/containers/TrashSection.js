import React from "react";

const styles = {
  wrapper: {
    verticalAlign: "center",
  }
};

export default class TrashSection extends React.Component {

  render() {
    return(
      <div style={styles.wrapper}>
        trash
      </div>
    );
  }

}
