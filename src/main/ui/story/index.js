import * as actions from "./actions";
import * as constants from "./constants";
import * as selectors from "./seletors";
import reducer from "./reducer";

export default { actions, constants, selectors, reducer };
