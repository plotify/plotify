// import { getPresenceHistoryId, setPresenceHistoryId } from './presence-history-id'

const addChange = async (database, type, characterId, typeId, historyId) => {
  // const prevHistoryId = await getPresenceHistoryId(database, type, typeId)
  // await setPresenceHistoryId(database, type, typeId, historyId)
}

export default addChange
